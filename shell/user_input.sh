#!/bin/bash

# Prompt user for their name
echo "What's your name?"
#read -p user_name [add -p for taking input in same line]
read user_name

# Greet the user using the entered name
echo "Hello, $user_name!"
